# Import fixtures

These files are **synthetic**, not records retrieved from eLIBRARY or a journal ranking service.
The mathematically valid ISSN is a teaching fixture and is not asserted to be assigned to a real journal.
Use a separate test database. Replace every field with authorized source data and the real source URL before real use.
Never import fixture classifications into the database used for an actual academic assessment.
